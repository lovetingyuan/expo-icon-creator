{
	"name": "expo-icon-creator",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npm run build && wrangler deploy",
		"dev": "vite",
		"build": "vite build",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"vite": "^6.0.0",
		"wrangler": "^4.69.0"
	},
	"dependencies": {
		"jszip": "^3.10.1"
	}
}
